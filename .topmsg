Subject: [PATCH] Regenerate all configure files with autoconf 2.68

From: Thomas Schwinge <thomas@schwinge.name>

---

    $ (PATH=$HOME/tmp/source/autoconf/v2.68.build.install/bin:$PATH; find -name configure.ac | while read f; do autoconf "$f" > "${f%.ac}" || echo >&2 "### $f failed"; done)

---
