Subject: [PATCH] Build pldd also if use-ldconfig != yes || build-shared != yes

From: Thomas Schwinge <thomas@schwinge.name>

2011-09-22  Thomas Schwinge  <thomas@schwinge.name>

	* elf/Makefile (pldd-modules): Define unconditionally.

---

If in config.make, ``use-ldconfig = no'':

    [...]/elf/pldd.o: In function `find_maps32':
    [...]/elf/pldd-xx.c:128: undefined reference to `xmalloc'
    [...]/elf/pldd.o: In function `find_maps64':
    [...]/elf/pldd-xx.c:128: undefined reference to `xmalloc'
    [...]/elf/pldd.o: In function `get_process_info':
    [...]/elf/pldd.c:216: undefined reference to `xrealloc'
    collect2: ld returned 1 exit status
    make[2]: *** [[...]/elf/pldd] Error 1
    make[2]: Leaving directory `[...]/elf'
    make[1]: *** [elf/others] Error 2

---
