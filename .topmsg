From: Samuel Thibault <samuel.thibault@ens-lyon.org>
Subject: [PATCH] Set version for global signal disposition functions

This adds the symbols needed by libpthread & such to the external interface.

The version here is just the current version, but it has to eventually be the version when the patch will actually be applied upstream.
